version: "3"
services:
  koroghlu:
    container_name: koroghlu-cuda
    entrypoint: "/run.sh"
    logging:
      driver: local
    build:
      context: .
      dockerfile: Dockerfile.cuda
    volumes:
      - ./Volume:${VOLUME}


